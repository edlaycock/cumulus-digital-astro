---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ServiceCards from '../components/ServiceCards';
import TestimonialCarousel from '../components/TestimonialCarousel';
import { EMAIL, PHONE_DISPLAY, PHONE_TEL, ADDRESS } from '../consts';

const services = (await getCollection('services'))
  .filter((s) => s.data.showOnHome)
  .sort((a, b) => a.data.order - b.data.order);

const serviceItems = [
  ...services.map((s) => ({
    title: s.data.title,
    summary: s.data.summary,
    iconPath: s.data.iconPath,
    href: `/services/#${s.id}`,
    linkText: 'Learn more →',
  })),
  {
    title: 'Travel Industry',
    summary:
      '25+ years of deep travel-sector expertise. We understand operators, agents, and travellers like no generic agency can.',
    iconPath: 'M10 32Q18 10 24 28Q30 46 40 20M20 14L24 8L28 14',
    href: '/about/#travel',
    linkText: 'Our story →',
  },
];

const work = (await getCollection('work')).sort((a, b) => a.data.order - b.data.order);
const testimonials = (await getCollection('testimonials')).map((t) => t.data);
---

<BaseLayout
  title="Cumulus Digital — Digital Marketing & Web Design, Surrey"
  description="Bespoke branding, hand-built websites and SEO for ambitious UK businesses. 25+ years travel industry experience. Based in Hersham, Walton-on-Thames, Surrey."
>
  <!-- ===================== HERO ===================== -->
  <section class="hero" id="home">
    <canvas id="hero-canvas" aria-hidden="true"></canvas>
    <div class="hero-scrim" aria-hidden="true"></div>

    <div class="hero-body">
      <div class="hero-copy">
        <p class="eyebrow reveal d1">
          <span class="eyebrow-dot"></span>
          Digital marketing &amp; web · Surrey, UK
        </p>

        <h1 class="headline reveal d2">
          It's about making<br />
          ideas <em class="glow">happen</em>.
        </h1>

        <p class="lede reveal d3">
          Bespoke branding, fast hand-built websites and search marketing
          for ambitious UK businesses — backed by 25&nbsp;years in the
          travel sector. Most clients see real impact within weeks.
        </p>

        <div class="cta-row reveal d4">
          <a class="btn btn--primary btn--lg" href="/contact/">Start a project →</a>
          <a class="btn btn--ghost btn--lg" href="/work/">See our work</a>
        </div>

        <ul class="trust reveal d5" aria-label="Trust signals">
          <li><span class="tick" aria-hidden="true">✦</span> Bespoke, hand-built websites</li>
          <li><span class="tick" aria-hidden="true">✦</span> 25+ yrs travel specialists</li>
          <li><span class="tick" aria-hidden="true">✦</span> Hersham, Surrey</li>
        </ul>
      </div>
    </div>

    <div class="hero-scroll-hint reveal d5" aria-hidden="true">
      <span class="scroll-line"></span>
      <span class="scroll-label">scroll</span>
    </div>
  </section>

  <!-- ===================== TRUST BAND ===================== -->
  <!-- TODO(owner): supply client logo files to turn this into a proper logo band -->
  <section class="logos-band" aria-label="Who we work with">
    <div class="container">
      <p class="logos-label">
        Trusted by Surrey businesses and travel brands — including Surrey Hills Surfacing
      </p>
    </div>
  </section>

  <!-- ===================== SERVICES ===================== -->
  <section class="section services-section" id="services">
    <div class="container">
      <div class="section-header">
        <p class="section-label">What we do</p>
        <h2 class="section-title">
          Everything you need<br />to grow online.
        </h2>
        <p class="section-sub">
          From first impression to lasting impact — strategy, design, code,
          and marketing that works together seamlessly.
        </p>
      </div>

      <ServiceCards items={serviceItems} client:visible />
    </div>
  </section>

  <!-- ===================== NUMBERS BAND ===================== -->
  <section class="numbers-band">
    <video
      class="numbers-video"
      autoplay
      muted
      loop
      playsinline
      aria-hidden="true"
    >
      <source src="/numbers-band.webm" type="video/webm" />
    </video>
    <div class="numbers-content">
      <div class="container">
        <ul class="numbers-row" role="list">
          <li>
            <strong class="number-val">25<span>+</span></strong>
            <span class="number-label">Years travel experience</span>
          </li>
          <li>
            <strong class="number-val">2015</strong>
            <span class="number-label">Serving Surrey since</span>
          </li>
          <li>
            <strong class="number-val">Weeks</strong>
            <span class="number-label">Typical time to impact</span>
          </li>
          <li>
            <strong class="number-val">Surrey</strong>
            <span class="number-label">Based in Hersham</span>
          </li>
        </ul>
      </div>
    </div>
  </section>

  <!-- ===================== VALUES ===================== -->
  <section class="section values-section">
    <div class="container">
      <div class="section-header">
        <p class="section-label">How we work</p>
        <h2 class="section-title">Our principles.</h2>
        <p class="section-sub">Four things we believe that shape everything we do.</p>
      </div>
      <div class="values-grid">
        <div class="value-card" data-reveal>
          <span class="value-num">01</span>
          <h3>We build, not bolt together</h3>
          <p>Custom code, not plugin stacks. Every site we build is hand-crafted for speed, security and longevity — so you never outgrow it.</p>
        </div>
        <div class="value-card" data-reveal style="--reveal-delay:.1s">
          <span class="value-num">02</span>
          <h3>We know your industry</h3>
          <p>25+ years inside the travel sector means we ask smarter questions and skip the expensive learning curve most agencies charge you for.</p>
        </div>
        <div class="value-card" data-reveal style="--reveal-delay:.2s">
          <span class="value-num">03</span>
          <h3>We move fast</h3>
          <p>Most clients see measurable impact within weeks, not quarters. We're built for momentum — not endless rounds of revisions and bureaucracy.</p>
        </div>
        <div class="value-card" data-reveal style="--reveal-delay:.3s">
          <span class="value-num">04</span>
          <h3>We stay partners</h3>
          <p>We don't disappear post-launch. We're on hand for ongoing growth, iteration and support — because your success is how we measure ours.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== WORK ===================== -->
  <section class="section work-section" id="work">
    <div class="container">
      <div class="section-header">
        <p class="section-label">Our work</p>
        <h2 class="section-title">Ideas made real.</h2>
        <p class="section-sub">
          Real projects for real businesses — and room on this page for yours.
        </p>
      </div>

      <ul class="work-grid" role="list">
        {work.map((item) => (
          <li class={`work-card${item.data.wide ? ' work-card--wide' : ''}${item.data.placeholder ? ' work-card--placeholder' : ''}`}>
            <div class="work-card-inner">
              <div class="work-card-visual" aria-hidden="true"></div>
              <span class="work-card-monogram" aria-hidden="true">
                {item.data.client.split(' ').map((w) => w[0]).slice(0, 3).join('')}
              </span>
              <div class="work-card-overlay"></div>
              <div class="work-card-content">
                <span class="work-tag">{item.data.services.join(' + ')}</span>
                <h3>{item.data.title}</h3>
                <p>{item.data.summary}</p>
                {item.data.url && (
                  <a class="btn btn--ghost" href={item.data.url} target="_blank" rel="noopener">
                    Visit the site →
                  </a>
                )}
                {item.data.placeholder && (
                  <a class="btn btn--ghost" href="/contact/">Start something like this →</a>
                )}
              </div>
            </div>
          </li>
        ))}
      </ul>

      <div class="work-cta">
        <a class="btn btn--primary btn--lg" href="/contact/">Discuss your project →</a>
      </div>
    </div>
  </section>

  <!-- ===================== TRAVEL SPECIALISM ===================== -->
  <section class="section travel-section" id="about">
    <div class="container travel-inner">
      <div class="travel-copy">
        <p class="section-label">25+ years in travel</p>
        <h2 class="section-title">
          We know your<br />industry from the inside.
        </h2>
        <p class="travel-body">
          Most digital agencies learn your sector from a brief. We've spent over
          two decades working inside the travel industry — across operations,
          marketing, product, IT and general management. That means we ask better
          questions, spot the right opportunities, and skip the expensive learning curve.
        </p>
        <ul class="travel-checklist">
          <li>Tour operators &amp; travel agents</li>
          <li>Destination marketing &amp; DMOs</li>
          <li>Hospitality &amp; accommodation brands</li>
          <li>Travel technology companies</li>
        </ul>
        <a class="btn btn--primary" href="/about/#travel">Talk to a travel specialist →</a>
      </div>

      <div class="travel-photo-wrap" data-reveal>
        <div class="brand-panel">
          <img src="/cumulus-logo.png" alt="" loading="lazy" />
        </div>
        <div class="travel-photo-badge">
          <strong>25+</strong>
          <span>Years in travel</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== SURREY GEO ===================== -->
  <section class="section surrey-section">
    <div class="container surrey-inner">
      <div class="surrey-photo-wrap" data-reveal>
        <div class="brand-panel brand-panel--short">
          <img src="/cumulus-logo.png" alt="" loading="lazy" />
        </div>
        <div class="travel-photo-badge">
          <strong>KT12</strong>
          <span>Hersham, Surrey</span>
        </div>
      </div>

      <div class="surrey-copy">
        <p class="section-label">Local to Surrey</p>
        <h2 class="section-title">
          Your local digital<br />agency in Surrey.
        </h2>
        <p class="travel-body">
          Based in Hersham, Walton-on-Thames, we serve ambitious businesses across
          Surrey and the South East. We know the local market, the competition, and
          the opportunities — and we bring national-agency quality to your doorstep.
        </p>
        <div class="surrey-areas">
          <span class="surrey-tag">Walton-on-Thames</span>
          <span class="surrey-tag">Hersham</span>
          <span class="surrey-tag">Esher</span>
          <span class="surrey-tag">Weybridge</span>
          <span class="surrey-tag">Woking</span>
          <span class="surrey-tag">Epsom</span>
          <span class="surrey-tag">Kingston</span>
          <span class="surrey-tag">London</span>
        </div>
        <a class="btn btn--primary" href="/contact/">Get a free consultation →</a>
      </div>
    </div>
  </section>

  <!-- ===================== TESTIMONIALS ===================== -->
  <section class="section testimonials-section">
    <div class="container">
      <div class="section-header">
        <p class="section-label">What clients say</p>
        <h2 class="section-title">Results speak first.</h2>
        <p class="section-sub">
          We measure success by what our clients achieve — not just what we deliver.
        </p>
      </div>
      <TestimonialCarousel items={testimonials} client:visible />
    </div>
  </section>

  <!-- ===================== CTA ===================== -->
  <section class="cta-section" id="contact">
    <div class="container">
      <div class="cta-inner">
        <div class="cta-glow" aria-hidden="true"></div>
        <p class="section-label">Ready to start?</p>
        <h2 class="cta-headline">
          Let's make your<br />idea <em class="glow">happen</em>.
        </h2>
        <p class="cta-sub">
          Tell us about your project — we'll come back within one working day.
        </p>
        <div class="cta-actions">
          <a class="btn btn--primary btn--lg" href="/contact/">
            Start a project →
          </a>
          <a class="btn btn--ghost btn--lg" href={`tel:${PHONE_TEL}`}>
            {PHONE_DISPLAY}
          </a>
        </div>
        <p class="cta-location">
          {ADDRESS.street}, {ADDRESS.locality}, {ADDRESS.region} {ADDRESS.postcode}
          &nbsp;·&nbsp; <a href={`mailto:${EMAIL}`} style="color:inherit">{EMAIL}</a>
        </p>
      </div>
    </div>
  </section>

  <script>
    import '../scripts/hero.js';
  </script>
</BaseLayout>
